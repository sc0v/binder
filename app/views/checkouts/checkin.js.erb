$("#barcode").focus();
$("#barcode").val("");

<% unless @checkout.nil? %>
$("#rapid-checkin-sidebar > tbody").append("<%= escape_javascript( render(:partial => "checkin_sidebar_entry")).html_safe %>");
<% end %>