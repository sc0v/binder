<% update_document_title(add: 'Tools') %>
<% update_breadcrumbs(add: 'Tools') %>
<%= nav(active: 'tools') %>
<% content_for :head do %>
<script>

</script>
<% end %>

<div class="content">
  <div>
    <h1>Tools</h1>
    <%= content_tag :div, data:
        { controller: 'table', config: tools_table_config } do %>
    Loading...
    <% end %>
    <div>
      <% if can?(:create, Tool) and @organization.blank? %>
        <%= link_to t('.new', default: t('helpers.links.new')),
                    new_tool_path,
                    class: 'btn btn-primary' %>
      <% end %>
    </div>
   </div><div class="sidebar">
    <%= render 'tools/checkouts/sidebar' %>
    <div class="">
        <h3>Tool type</h3>
        <ul>
            <li>
                <%= link_to 'Tool', tools_path(type: 'tool') %>
            </li>
            <li>
                <%= link_to 'Hardhat', tools_path(type: 'hardhat') %>
            </li>
            <li>
                <%= link_to 'Radio', tools_path(type: 'radio') %>
            </li>
        </ul>
    </div>
  </div>
</div>
